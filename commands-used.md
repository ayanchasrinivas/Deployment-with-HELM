# Install HELM Plugins required.
1) helm plugin install https://github.com/databus23/helm-diff --verify=false
2) helm plugin install https://github.com/helm-unittest/helm-unittest
3) helm plugin list

# Plugin installation through winget
4) winget install --id Microsoft.PowerShell -e
